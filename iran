<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <title>دانلود نرم افزار</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- فونت فارسی -->
    <link href="https://fonts.googleapis.com/css2?family=Vazirmatn:wght@300;400;600&display=swap" rel="stylesheet">

    <style>
        * {
            box-sizing: border-box;
            font-family: "Vazirmatn", sans-serif;
        }

        body {
            margin: 0;
            min-height: 100vh;
            background: linear-gradient(180deg, #1f3b44, #244d57);
            display: flex;
            align-items: center;
            justify-content: center;
            color: #fff;
        }

        .card {
            background: rgba(255, 255, 255, 0.08);
            backdrop-filter: blur(6px);
            border-radius: 16px;
            padding: 40px 30px;
            width: 90%;
            max-width: 420px;
            text-align: center;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.25);
        }

        .logo {
            width: 120px;
            height: 120px;
            background: #fff;
            border-radius: 50%;
            margin: 0 auto 20px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .logo img {
            width: 80%;
            height: auto;
        }

        h1 {
            margin: 10px 0 15px;
            font-size: 24px;
            font-weight: 600;
        }

        p {
            font-size: 14px;
            opacity: 0.9;
            margin-bottom: 30px;
            line-height: 1.8;
        }

        .btn-download {
            display: inline-block;
            background: linear-gradient(135deg, #1da1f2, #007bff);
            color: #fff;
            text-decoration: none;
            padding: 14px 32px;
            border-radius: 10px;
            font-size: 16px;
            font-weight: 500;
            transition: transform 0.2s ease, box-shadow 0.2s ease;
            box-shadow: 0 10px 20px rgba(0, 123, 255, 0.35);
        }

        .btn-download:hover {
            transform: translateY(-2px);
            box-shadow: 0 14px 25px rgba(0, 123, 255, 0.5);
        }

        footer {
            margin-top: 30px;
            font-size: 12px;
            opacity: 0.7;
        }
    </style>
</head>
<body>

    <div class="card">
        <div class="logo">
            <!-- لوگو -->
            <img src="logo.png" alt="لوگو">
        </div>

        <h1>دانلود اپلیکیشن</h1>

        <p>
            برای دریافت نرم‌افزار، روی دکمه زیر کلیک کنید.
        </p>

        <a href="#" class="btn-download">
            دانلود اپلیکیشن
        </a>

        <footer>
            © ۲۰۲۵ - تمامی حقوق محفوظ به دانشگاه صنعتی بیرجند میباشد
        </footer>
    </div>

</body>
</html>
